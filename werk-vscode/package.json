{
  "name": "werk",
  "displayName": "Werk",
  "description": "Werk language and build system support",
  "repository": "https://github.com/simonask/werk",
  "license": "MIT OR Apache-2.0",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.96.0"
  },
  "scripts": {
    "vsce": "vsce",
    "package": "vsce package",
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "vscode:prepublish": "tsc && node esbuild.mjs --production",
    "check": "tsc"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "out/main.js",
  "contributes": {
    "languages": [{
      "id": "werk",
      "aliases": ["Werk", "werk"],
      "extensions": [".werk", "Werkfile", "werkfile"],
      "configuration": "./language-configuration.json"
    }],
    "grammars": [{
      "language": "werk",
      "scopeName": "source.werk",
      "path": "./syntaxes/werk.tmLanguage.json"
    }],
    "configuration": {
      "properties": {
        "werk.enableCodeLens": {
          "type": "boolean",
          "default": true
        }
      }
    },
    "taskDefinitions": [
      {
        "type": "werk",
        "required": ["kind", "target"],
        "properties": {
          "kind": {
            "type":"string",
            "enum": ["task", "build"]
          },
          "target": {
            "type": "string"
          }
        },
        "when": "shellExecutionSupported"
      }
    ]
  },
  "devDependencies": {
    "@vscode/vsce": "3.2.2",
    "@types/node": "^22.13.1",
    "@types/vscode": "^1.96.0",
    "esbuild": "^0.24.2"
  },
  "dependencies": {
  }
}
